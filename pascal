{-
programa le dois numeros inteiros e encontra o maior
-}
program exemplo2;
	integer num_1, num_2;
	integer maior;
begin
	read(num_1); # imprime o valor
	read(num_2);
	if num_1 > num_2:
		maior = num_1
	elif
		maior = num_2;
	write(maior)
end.
